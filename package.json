{
  "name": "wera-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspace=@wera/db && npm run build --workspace=web"
  },
  "devDependencies": {
    "next": "14.1.0"
  }
}
